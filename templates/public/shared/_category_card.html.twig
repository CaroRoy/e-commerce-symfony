 <div class="card categoryCard">
    <a href="{{path('show_category_products', {'slug' : category.slug})}}">
        <img
                {% if category.imageUrl is same as null %}
                    src="{{ asset('images/image-default.jpg') }}" alt="image cat̩gorie"
                {% else %}
                    src="{{ category.imageUrl }}"
                {% endif %}
                alt="{{ category.name }}"
                class="img-fluid"
        />
    </a>
    <div class="card-body categoryCardBody">
        <a href="{{path('show_category_products', {'slug' : category.slug})}}" class="btn btn-primary" style="width:100%;">
            Consulter {{ category.name }}
        </a>
    </div>
</div>